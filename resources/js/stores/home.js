import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const homeStore = {
    namespaced: true,
    state: {
    },
    mutations: {
    },
    actions: {
    }
};

export default homeStore;
